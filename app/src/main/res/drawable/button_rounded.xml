<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow -->
    <item android:top="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/small_button_corner_radius" />
            <solid android:color="@color/shadow_light" />
        </shape>
    </item>
    <!-- Button -->
    <item android:bottom="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/small_button_corner_radius" />
            <gradient
                android:startColor="@color/primary_blue"
                android:endColor="@color/primary_indigo"
                android:angle="90" />
            <stroke
                android:width="2dp"
                android:color="@color/primary_purple" />
        </shape>
    </item>
</layer-list>